import { createTheme } from "@mui/material";

const ustTheme = createTheme({
  palette: {
    primary: {
      main: "#003366",
    },
    secondary: {
      main: "#996600",
    },
    error: {
      main: "#ed1b2f",
    },
  },
});

export default ustTheme;
